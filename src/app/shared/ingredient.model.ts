export class Ingredient{
    // public name:string;
    // public amount: number;
    // writing like this it shows the same effect as written in before.(Public keyword)

    constructor(public name:string,public  amount:number){
        // this.name = name;
        // this.amount = amount;
    }
}